import { useAppSelector } from './redux';

// export function useAuth() {
//   const { email, token, id, isAuth } = useAppSelector((state) => state.authReducer.isAuth);
//   return { email, token, id, isAuth };
// }
